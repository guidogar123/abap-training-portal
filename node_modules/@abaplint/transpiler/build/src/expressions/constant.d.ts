import { Nodes } from "@abaplint/core";
import { Chunk } from "../chunk";
import { Traversal } from "../traversal";
import { IExpressionTranspiler } from "./_expression_transpiler";
export declare class ConstantTranspiler implements IExpressionTranspiler {
    private readonly addGet;
    constructor(addGet?: boolean);
    private handleInteger;
    transpile(node: Nodes.ExpressionNode, traversal: Traversal): Chunk;
    private handleCharacter;
    static escape(str: string): string;
}
